# 后台数据维护界面与业务展示界面数据映射逻辑文档

## 一、文档概述

### 1.1 文档目的

本文档定义新能源电力交易辅助决策系统中，后台数据维护界面与业务展示界面之间的数据映射关系，为后端开发人员提供清晰的数据流转规范和接口设计依据。

### 1.2 适用范围

本文档适用于系统所有模块的后端开发，包括但不限于：
- 后台数据维护模块（场站信息、合约管理、SCADA数据、现货数据等）
- 业务展示模块（总览分析、预测信息、中长期交易、现货交易）
- 系统管理模块（用户管理、数据字典、操作日志等）

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| 后台维护界面 | 运维人员进行数据录入、修改、删除、导入导出的页面 |
| 业务展示界面 | 交易员、预测分析师查看数据、分析图表、制定策略的页面 |
| 数据映射 | 后台维护的数据与业务展示的数据之间的对应关系 |
| 单一数据源 | 数据只在一个地方维护，多处读取使用 |
| 软删除 | 删除操作不物理删除，而是标记删除状态 |

---

## 二、数据架构总览

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           数据架构总览图                                   │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────────────┐           ┌──────────────────────────────┐ │
│  │        后台维护界面        │           │         业务展示界面          │ │
│  │  ┌──────────────────────┐ │           │  ┌────────────────────────┐  │ │
│  │  │ 场站信息管理         │ │           │  │ 总览分析               │  │ │
│  │  │ 合约管理             │ │           │  │  •市场行情             │  │ │
│  │  │ SCADA实时数据管理    │ │           │  │  •复盘看板             │  │ │
│  │  │ 现货数据管理         │ │           │  ├────────────────────────┤  │ │
│  │  │ 清算结算数据管理     │ │           │  │ 预测信息               │  │ │
│  │  │ 预测数据管理         │ │           │  │  •气象信息看板         │  │ │
│  │  │ 算法模型管理         │ │           │  │  •功率预测精度分析     │  │ │
│  │  │ 市场交易数据管理     │ │           │  │  •负荷预测             │  │ │
│  │  │ 系统管理             │ │           │  │  •电价预测             │  │ │
│  │  │  •用户管理           │ │           │  ├────────────────────────┤  │ │
│  │  │  •数据字典           │ │           │  │ 中长期交易             │  │ │
│  │  │  •操作日志           │ │           │  │  •中长期持仓           │  │ │
│  │  │  •系统参数配置       │ │           │  │  •中长期辅助决策       │  │ │
│  │  └──────────────────────┘ │           │  ├────────────────────────┤  │ │
│  │                           │           │  │ 现货交易               │  │ │
│  │         ↓ 写入/更新        │           │  │  •现货市场             │  │ │
│  └──────────────────────────┘           │  │  •价格日历             │  │ │
│                         │               │  │  •现货辅助决策         │  │ │
│                         ↓               │  └────────────────────────┘  │ │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                          数据库层                                   │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌───────────┐  │ │
│  │  │ station_info │ │contract_info│ │ scada_data   │ │ spot_data │  │ │
│  │  │ (场站信息)   │ │ (合约信息)   │ │ (SCADA数据)  │ │(现货数据) │  │ │
│  │  ├──────────────┤ ├──────────────┤ ├──────────────┤ ├───────────┤  │ │
│  │  │settlement_  │ │prediction_  │ │algorithm_    │ │data_dict  │  │ │
│  │  │   data       │ │   data       │ │   model      │ │(数据字典) │  │ │
│  │  │ (结算数据)   │ │ (预测数据)   │ │ (算法模型)   │ ├───────────┤  │ │
│  │  ├──────────────┤ ├──────────────┤ ├──────────────┤ │system_    │  │ │
│  │  │market_trade  │ │operation_    │ │import_task   │ │ config    │  │ │
│  │  │   data       │ │   log        │ │  record      │ │(系统配置) │  │ │
│  │  │(市场交易数据)│ │ (操作日志)   │ │ (导入任务)   │ ├───────────┤  │ │
│  │  ├──────────────┤ ├──────────────┤ ├──────────────┤ │strategy_  │  │ │
│  │  │  weather_    │ │model_dock   │ │              │ │   info     │  │ │
│  │  │   data       │ │    ing       │ │              │ │ (策略信息) │  │ │
│  │  │  (气象数据)  │ │ (模型对接)   │ │              │ └───────────┘  │ │
│  │  └──────────────┘ └──────────────┘ └──────────────┘                │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                         ↑                                              │
│                         │ 爬虫/API自动写入                               │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                          外部数据源                                   │ │
│  │  省电力交易中心  │  国家气象中心  │  SCADA系统  │  其他外部系统       │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流向说明

| 方向 | 流程 | 说明 |
|------|------|------|
| 写入 | 后台维护界面 → 数据库 | 人工录入、导入、编辑、删除 |
| 写入 | 爬虫/API → 数据库 | 定时任务自动获取外部数据 |
| 写入 | 算法计算 → 数据库 | 预测结果、策略生成等系统计算 |
| 读取 | 数据库 → 业务展示界面 | 查询、聚合、关联后展示 |

---

## 三、数据映射关系表

### 3.1 核心业务数据映射

| 序号 | 后台维护页面 | 维护数据 | 数据库表 | 业务展示页面 | 展示方式 |
|------|--------------|----------|----------|--------------|----------|
| 1 | 场站信息管理 | 场站名称、装机容量、最小技术出力、爬坡率、启停约束 | station_info | 所有页面 | 下拉选项、基础数据关联 |
| 2 | 合约管理 | 合约名称、交易品类、交易时段、电量占比、电价、年度、所属公司、关联场站、备注 | contract_info | 中长期持仓、中长期辅助决策 | 合约电量堆叠图、持仓统计、余量分析 |
| 3 | SCADA实时数据管理 | 日期、时段(15min)、场站名称、机组编号、上网功率(MW)、预测功率(MW)、状态 | scada_data | 功率预测精度分析、现货市场 | 实际出力曲线、偏差分析、实时监控 |
| 4 | 现货数据管理 | 交易日期、交易时段、节点名称、日前出清电价、实时出清电价、日前出清电量、实时出清电量、电源类型、日前出清台数、实时出清台数 | spot_data | 现货市场、价格日历、现货辅助决策 | 出清电价曲线、价格热力图、收益计算 |
| 5 | 清算结算数据管理 | 结算月份、结算单号、结算类型、交易电量、结算电价、结算金额、场站名称、结算状态 | settlement_data | 复盘看板 | 结算单对比、收益统计、偏差分析 |
| 6 | 预测数据管理 | 预测日期、预测类型、预测值、置信区间、偏差率 | prediction_data | 负荷预测、电价预测、功率预测精度分析 | 预测曲线、置信区间、准确率趋势 |
| 7 | 算法模型管理 | 算法名称、算法类型、训练节点、训练数据集、模型参数 | algorithm_model | 负荷预测、电价预测 | 算法方案选择、预测任务触发 |
| 8 | 市场交易数据管理 | PDF文件、报告月份、上传人、上传时间 | market_trade_data | 总览分析-市场行情 | PDF下载、市场信息披露 |
| 9 | 现货辅助决策 | 策略名称、策略类型、风险等级、关联场站、报价日期、策略数据、预期收益 | strategy_info | 现货辅助决策、中长期辅助决策 | 策略方案保存、载入历史方案 |

### 3.2 系统配置数据映射

| 序号 | 后台维护页面 | 维护数据 | 数据库表 | 业务展示页面 | 展示方式 |
|------|--------------|----------|----------|--------------|----------|
| 10 | 数据字典 | 字典类型、字典编码、字典名称、排序号、状态 | data_dictionary | 所有页面 | 下拉选项、状态标签、枚举值映射 |
| 11 | 系统参数配置 | 参数编码、参数名称、参数值、参数说明、参数分类 | system_config | 所有页面 | 全局配置读取、系统参数引用 |
| 12 | 用户管理 | 用户名、密码、角色、所属公司、状态 | user_info | 所有页面 | 权限控制、用户身份标识 |
| 13 | 操作日志 | 操作人、操作时间、操作类型、操作内容、操作结果 | operation_log | 操作日志页面 | 日志查询、审计追溯 |
| 14 | 导入任务记录 | 文件名、文件大小、上传时间、导入状态、成功数、失败数 | import_task_record | 导入任务记录页面 | 任务列表、失败日志查看 |
| 15 | 数据回收站 | 原业务表、原数据ID、删除时间、删除人 | data_recycle_bin | 数据回收站页面 | 删除数据恢复、永久删除 |

---

## 四、数据写入模式

### 4.1 模式一：人工录入/导入

#### 适用场景
- 基础配置数据（场站信息、合约信息、结算单）
- 非结构化数据（PDF报告）
- 数据补录

#### 流程说明

```
运维人员 → 后台维护页面 → 表单校验/文件解析 → 数据校验（必填、格式、枚举） → 写入数据库 → 记录操作日志
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 表单校验 | 必填字段检查、数据类型检查、格式检查 |
| 文件解析 | 支持.xlsx、.xls、.csv格式，Excel需解析两个Sheet |
| 数据校验 | 枚举值校验、数据范围校验、唯一键校验 |
| 事务控制 | 批量导入失败时回滚，保证数据一致性 |
| 操作日志 | 记录操作人、操作时间、操作类型、操作内容 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/station/save | POST | 新增/编辑场站信息 |
| /api/contract/import | POST | 批量导入合约数据 |
| /api/settlement/save | POST | 新增/编辑结算单 |

### 4.2 模式二：爬虫/API自动获取

#### 适用场景
- 市场公开数据（清算电价、现货出清数据）
- 气象数据
- SCADA实时数据

#### 流程说明

```
定时任务 → 爬虫/API调用 → 数据解析/清洗 → 字段映射 → 数据校验 → 写入数据库 → 记录任务日志
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 定时执行 | 根据数据更新频率设置定时任务（每15分钟、每日等） |
| 数据解析 | HTML解析、JSON解析、Excel文件解析 |
| 数据清洗 | 去噪、格式统一、单位转换、空值处理 |
| 字段映射 | 外部字段名称 → 系统字段名称 |
| 增量更新 | 避免重复写入，根据唯一键判断是否更新 |
| 异常处理 | 爬取失败重试、接口超时处理、数据异常告警 |
| 日志记录 | 记录任务执行时间、数据量、成功率、失败原因 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/spider/clear-electricity | POST | 爬取清算电价电量数据 |
| /api/spider/spot-data | POST | 爬取现货出清数据 |
| /api/spider/weather-data | POST | 爬取气象数据 |

### 4.3 模式三：系统计算生成

#### 适用场景
- 功率预测结果
- 负荷预测结果
- 电价预测结果
- 收益分析结果
- 交易策略生成

#### 流程说明

```
触发条件（手动/定时） → 调用算法服务 → 计算结果 → 数据校验 → 写入数据库 → 通知下游模块
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 触发方式 | 手动触发（用户点击按钮）、定时触发（系统定时任务） |
| 算法调用 | 通过RPC或HTTP调用算法服务 |
| 结果写入 | 计算完成后写入对应的预测数据表 |
| 通知机制 | 预测完成后通知下游模块（如现货辅助决策） |
| 历史记录 | 保留所有历史预测结果，查询时默认展示最新结果 |
| 失败处理 | 计算失败时记录错误日志，提示用户重试 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/prediction/power | POST | 触发功率预测 |
| /api/prediction/load | POST | 触发负荷预测 |
| /api/prediction/price | POST | 触发电价预测 |
| /api/strategy/generate | POST | 生成交易策略 |

---

## 五、数据读取模式

### 5.1 模式一：直接查询

#### 适用场景
- 列表数据展示
- 详情数据查看
- 配置数据读取

#### 流程说明

```
业务页面 → API请求 → SQL查询 → 返回原始数据 → 前端渲染
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 分页支持 | 大数据量列表需支持分页查询 |
| 筛选支持 | 支持按关键字段筛选 |
| 排序支持 | 支持按关键字段排序 |
| 权限控制 | 根据用户角色过滤数据 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/station/list | GET | 查询场站列表 |
| /api/contract/detail | GET | 查询合约详情 |
| /api/data-dict/list | GET | 查询数据字典 |

### 5.2 模式二：聚合计算

#### 适用场景
- 统计指标计算
- 数据汇总展示
- 趋势分析

#### 流程说明

```
业务页面 → API请求 → SQL聚合查询 → 计算统计结果 → 返回聚合数据 → 前端渲染图表
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 聚合函数 | 使用SUM、AVG、COUNT、MAX、MIN等聚合函数 |
| 分组统计 | 支持按时间、品类、场站等维度分组 |
| 性能优化 | 复杂聚合查询需添加索引或使用缓存 |
| 数据缓存 | 统计结果可缓存，定时刷新 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/contract/statistics | GET | 统计各品种合约电量 |
| /api/settlement/compare | GET | 结算单对比分析 |
| /api/prediction/accuracy | GET | 预测准确率统计 |

### 5.3 模式三：关联查询

#### 适用场景
- 多表关联数据展示
- 业务流程数据展示
- 综合分析数据

#### 流程说明

```
业务页面 → API请求 → SQL关联查询 → 多表JOIN → 返回关联数据 → 前端渲染
```

#### 具体要求

| 要求 | 说明 |
|------|------|
| 表关联 | 使用INNER JOIN、LEFT JOIN等关联方式 |
| 关联字段 | 通过外键字段关联（如station_id、contract_id） |
| 性能优化 | 控制关联表数量，添加关联字段索引 |
| 数据完整性 | 处理关联数据缺失情况 |

#### 示例接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/spot/decision | GET | 关联现货数据、功率预测、场站参数 |
| /api/midterm/decision | GET | 关联合约数据、功率预测、电价预测 |
| /api/review/dashboard | GET | 关联结算数据、合约数据、现货数据 |

---

## 六、关键设计原则

### 6.1 单一数据源原则

```
数据只在一个地方维护，多处读取使用，确保数据一致性。

示例：
- 场站信息只在「场站信息管理」页面维护
- 合约信息只在「合约管理」页面维护
- 所有业务页面通过外键关联读取，不重复存储
```

### 6.2 数据字典标准化

```
所有枚举值统一维护在数据字典中，确保全系统使用同一套编码。

示例：
- 合约类型：年度双边协商合约、月度集中竞价合约、月度双边协商合约、季度双边协商合约、年度集中竞价合约、现货
- 结算状态：待确认、已确认、已结算、已对账
- 电源类型：光伏、风电、水电、燃煤火电、燃气火电、独立储能、生物质垃圾、其他
- 算法类型：时序预测模型、回归预测模型、神经网络模型、集成学习模型、混合架构、基线模型、机器学习模型、深度学习模型
```

### 6.3 软删除机制

```
删除操作不物理删除数据，而是标记删除状态，支持数据恢复。

设计要求：
- 所有业务表需包含字段：deleted_flag（0=正常，1=已删除）、deleted_time（删除时间）、deleted_by（删除人）
- 查询时自动过滤deleted_flag=0的数据
- 数据回收站页面可查看已删除数据，支持恢复或永久删除
- 配置类数据（场站、合约）支持恢复，时序数据（SCADA、现货）不支持恢复
```

### 6.4 操作日志记录

```
所有数据变更操作必须记录操作日志，便于追溯和审计。

记录内容：
- 操作人（user_id、user_name）
- 操作时间（operation_time）
- 操作类型（新增、编辑、删除、导入、导出）
- 操作模块（场站信息管理、合约管理等）
- 操作内容（修改前后的值对比）
- 操作结果（成功、失败）
- 失败原因（如果失败）
```

### 6.5 数据校验规则

```
所有写入数据必须经过严格校验，确保数据质量。

校验类型：
- 必填校验：关键字段不能为空
- 格式校验：日期格式、数值格式、邮箱格式等
- 范围校验：数值范围、时间范围
- 枚举校验：必须是数据字典中定义的值
- 唯一键校验：避免重复数据
- 关联校验：关联的外键必须存在
```

### 6.6 接口统一规范

```
所有API接口遵循统一规范，确保接口设计一致。

接口规范：
- URL格式：/api/{module}/{action}
- 请求格式：JSON，UTF-8编码
- 响应格式：统一JSON格式，包含code、message、data字段
- 错误码：ERR_xxx格式，统一错误码体系
- 分页参数：pageNum、pageSize
- 排序参数：orderBy、orderDirection
```

---

## 七、数据字典映射

### 7.1 核心数据字典项

| 字典类型 | 字典编码 | 字典名称 | 说明 |
|----------|----------|----------|------|
| contract-type | annual-bilateral | 年度双边协商合约 | 合约类型枚举 |
| contract-type | monthly-auction | 月度集中竞价合约 | 合约类型枚举 |
| contract-type | monthly-bilateral | 月度双边协商合约 | 合约类型枚举 |
| contract-type | quarterly-bilateral | 季度双边协商合约 | 合约类型枚举 |
| contract-type | annual-auction | 年度集中竞价合约 | 合约类型枚举 |
| contract-type | spot | 现货 | 合约类型枚举 |
| power-type | photovoltaic | 光伏 | 电源类型枚举 |
| power-type | wind | 风电 | 电源类型枚举 |
| power-type | hydro | 水电 | 电源类型枚举 |
| power-type | coal-fire | 燃煤火电 | 电源类型枚举 |
| power-type | gas-fire | 燃气火电 | 电源类型枚举 |
| power-type | storage | 独立储能 | 电源类型枚举 |
| power-type | biomass | 生物质垃圾 | 电源类型枚举 |
| power-type | other | 其他 | 电源类型枚举 |
| algorithm-type | time-series | 时序预测模型 | 算法类型枚举 |
| algorithm-type | regression | 回归预测模型 | 算法类型枚举 |
| algorithm-type | neural-network | 神经网络模型 | 算法类型枚举 |
| algorithm-type | ensemble | 集成学习模型 | 算法类型枚举 |
| algorithm-type | hybrid | 混合架构 | 算法类型枚举 |
| settlement-status | pending | 待确认 | 结算状态枚举 |
| settlement-status | confirmed | 已确认 | 结算状态枚举 |
| settlement-status | settled | 已结算 | 结算状态枚举 |
| settlement-status | reconciled | 已对账 | 结算状态枚举 |

### 7.2 数据字典使用规范

```
1. 所有下拉选择框的选项必须从数据字典中获取
2. 前端页面加载时，先查询数据字典，再渲染下拉选项
3. 后端接口接收枚举值时，校验是否在数据字典中存在
4. 数据字典新增或修改后，系统自动同步，无需重启
```

---

## 八、接口规范

### 8.1 接口响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1672531200000
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | Integer | 响应码，200=成功，其他=失败 |
| message | String | 响应消息 |
| data | Object | 响应数据 |
| timestamp | Long | 响应时间戳 |

### 8.2 错误码体系

| 错误码 | 说明 |
|--------|------|
| ERR_SYSTEM | 系统错误 |
| ERR_PARAM | 参数错误 |
| ERR_AUTH | 权限错误 |
| ERR_DATA_NOT_FOUND | 数据不存在 |
| ERR_DATA_DUPLICATE | 数据重复 |
| ERR_IMPORT_FAILED | 导入失败 |
| ERR_FILE_FORMAT | 文件格式错误 |
| ERR_FILE_SIZE | 文件大小超限 |

### 8.3 分页响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [],
    "pageNum": 1,
    "pageSize": 20,
    "total": 100
  },
  "timestamp": 1672531200000
}
```

---

## 九、示例说明

### 9.1 合约数据映射示例

#### 数据写入流程

```
运维人员 → 合约管理页面 → 导入Excel文件 → 解析文件 → 校验数据 → 写入contract_info表 → 记录操作日志

contract_info表字段：
- contract_id (主键)
- contract_name (合约名称)
- contract_type (合约类型，关联数据字典contract-type)
- trading_period (交易时段)
- volume_ratio (电量占比%)
- price (电价，元/MWh)
- year (年度)
- company_id (所属公司，关联company_info表)
- station_id (关联场站，关联station_info表)
- remark (备注)
- create_time (创建时间)
- create_by (创建人)
- update_time (更新时间)
- update_by (更新人)
- deleted_flag (删除标记)
- deleted_time (删除时间)
- deleted_by (删除人)
```

#### 数据读取流程

```
中长期持仓页面 → /api/contract/statistics → SQL聚合查询 → 返回统计数据 → 前端渲染图表

统计维度：
- 按交易品类分组：优先发电、双边协商、集中竞价、挂牌交易、绿电交易、合同转让
- 按月份分组：1月-12月
- 计算指标：合约电量合计、合约电费合计、加权平均电价

中长期辅助决策页面 → /api/midterm/decision → SQL关联查询 → 返回关联数据 → 前端渲染

关联表：
- contract_info：合约数据
- prediction_data：功率预测数据
- station_info：场站参数
```

### 9.2 现货数据映射示例

#### 数据写入流程

```
爬虫程序 → 定时任务 → 爬取省电力交易中心网站 → 解析HTML → 清洗数据 → 字段映射 → 写入spot_data表 → 记录任务日志

spot_data表字段：
- spot_id (主键)
- trade_date (交易日期)
- trade_period (交易时段，15分钟间隔)
- node_name (节点名称)
- day_ahead_price (日前出清电价，元/MWh)
- real_time_price (实时出清电价，元/MWh)
- day_ahead_volume (日前出清电量，MWh)
- real_time_volume (实时出清电量，MWh)
- power_type (电源类型，关联数据字典power-type)
- day_ahead_count (日前出清台数)
- real_time_count (实时出清台数)
- create_time (创建时间)
```

#### 数据读取流程

```
现货市场页面 → /api/spot/market → SQL查询 → 返回现货数据 → 前端渲染出力及价格曲线

价格日历页面 → /api/spot/calendar → SQL聚合查询 → 计算每日加权均价 → 前端渲染热力图

现货辅助决策页面 → /api/spot/decision → SQL关联查询 → 返回关联数据 → 前端渲染策略图表

关联表：
- spot_data：现货出清数据
- prediction_data：功率预测、电价预测数据
- station_info：场站参数
```

---

## 十、总结

### 10.1 核心逻辑

```
后台维护界面负责"写"数据，业务展示界面负责"读"数据，数据库是中间的桥梁。

数据写入三种方式：
1. 人工录入/导入：运维人员通过后台维护页面操作
2. 爬虫/API自动获取：定时任务从外部数据源获取
3. 系统计算生成：算法模型计算结果写入

数据读取三种方式：
1. 直接查询：获取原始数据列表
2. 聚合计算：统计分析数据
3. 关联查询：多表关联数据
```

### 10.2 开发要点

| 要点 | 说明 |
|------|------|
| 数据库设计 | 按照本文档定义的表结构设计数据库 |
| 接口开发 | 按照本文档定义的接口规范开发API |
| 数据校验 | 所有写入数据必须经过严格校验 |
| 操作日志 | 所有数据变更操作必须记录日志 |
| 错误处理 | 统一错误码体系，友好错误提示 |
| 性能优化 | 大数据量查询需添加索引和分页 |

### 10.3 协作要点

| 角色 | 职责 |
|------|------|
| 产品经理 | 定义数据映射关系、业务规则、接口需求 |
| 后端开发 | 实现数据库设计、接口开发、数据处理逻辑 |
| 前端开发 | 实现页面展示、数据渲染、用户交互 |
| 运维人员 | 部署系统、维护爬虫任务、监控数据质量 |

---

**文档版本**: V1.0  
**编制日期**: 2026年7月  
**编制人**: 产品经理