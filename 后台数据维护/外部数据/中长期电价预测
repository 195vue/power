# 中长期电价预测模块 — 开发文档

> 面向开发人员。涵盖模块定位、页面位置、数据接口、预测算法与公式、模型择优逻辑、字段字典与边界规则。
> 版本：v1.0　适用页面：预测信息 / 电价预测

---

## 1. 模块定位与整体目标

本模块是「预测信息 → 电价预测」页面下的一个**新增图表卡片**，用于展示**新能源中长期均价的月度趋势与外推预测**：

> 显示当前月之前 N 个月（默认 6）的**真实中长期均价**，并向后预测 M 个月（默认 6）的**中长期均价及置信区间**。

与页面已有的「未来 7 天现货价格预测」是互补关系，不是替代：

| 对比项 | 未来7天现货预测（既有） | 中长期电价预测（本模块，新增） |
|----|----|----|
| 时间粒度 | 日 | 月 |
| 时间跨度 | 未来 7 天 | 过去 6 月 + 未来 6 月 |
| 价格口径 | 现货最高/最低/均价 | 中长期加权均价 |
| 用途 | 现货报量报价参考 | 月度/多月/年度中长期签约时机判断 |

本模块与「中长期辅助决策」模块也存在数据关系：辅助决策模块中，集中竞价类交易方式（月度/多月/年度）的参考价来自品种成交均价（见辅助决策开发文档 §6.2），本模块的中长期趋势预测可作为该参考价的中长期锚点输入。

---

## 2. 页面位置与布局

在电价预测页面**「未来 7 天现货价格预测」图表 + 日表之间**（截图红框处）插入两张卡片：

| 顺序 | 卡片 | 容器 ID | 职责 |
|----|----|----|----|
| — | （既有）未来7天现货预测图 | — | 位置不变 |
| ★ | **中长期电价预测（月度）折线图** | `#mlPriceChart` | 历史实际实线 + 预测虚线 + 置信区间填充带 + 预测起点分界线 |
| ★ | **中长期电价预测明细表** | `#mlPriceTable` | 逐月中长期均价、区间上下限、环比 |
| — | （既有）现货预测日表 | — | 位置不变 |

图表沿用系统既有风格：**纯 SVG 绘制、零图表库依赖**（与辅助决策原型一致，见其开发文档 §11）。

---

## 3. 核心概念与术语

| 术语 | 符号 | 说明 |
|----|----|----|
| 中长期均价 | `Price_ml[t]` | 第 t 月新能源中长期市场加权平均成交价（元/MWh），来自交易/结算系统披露 |
| 基准月 | `asOfMonth` | 预测发起的当前月（YYYY-MM），其之前为历史、其起为预测 |
| 回看月数 | `lookback` | 图上展示的历史月数（默认 6），仅影响展示，模型用全量历史 |
| 预测月数 | `horizon` | 向后外推的月数（默认 6） |
| 置信区间 | `[low, high]` | 预测点的不确定性区间，默认 80%（z=1.28），随步长展宽 |
| 回测 MAE | — | 滚动一步预测的平均绝对误差，用于模型择优 |

---

## 4. 数据接口设计

> 建议算法核心在后端实现并暴露接口；前端可用等价的轻量预算做交互即时预览，
> 但**展示/保存以后端为准，避免前后端公式漂移**（与辅助决策模块同一原则）。

### 4.1 查询历史中长期均价

**`GET /api/midlong/price/history`**

| 参数 | 类型 | 必填 | 说明 |
|----|----|----|----|
| companyId | string | 是 | 场站/公司标识；可传全省口径标识 |
| asOfMonth | string(YYYY-MM) | 是 | 基准月，返回其之前（含）的历史 |
| lookback | int | 否 | 返回最近多少月，默认 6 |

响应体：

```json
{
  "companyId": "全省口径",
  "unit": "元/MWh",
  "points": [
    { "ym": "2025-12", "price": 380.74, "isForecast": false, "low": null, "high": null },
    { "ym": "2026-01", "price": 436.69, "isForecast": false, "low": null, "high": null }
  ]
}
```

> **数据来源**：`points[].price` ← 交易/结算系统的月度新能源中长期加权成交均价。
> 若分场站数据不足，可先用全省口径上线，`companyId` 预留分场站切换能力。

### 4.2 生成预测

**`POST /api/midlong/price/forecast`**

请求体：

```json
{
  "asOfMonth": "2026-06",
  "companyId": "全省口径",
  "horizon": 6,
  "lookback": 6,
  "confZ": 1.28,
  "forceModel": null,
  "history": null
}
```

| 字段 | 类型 | 说明 |
|----|----|----|
| asOfMonth | string | 基准月，其之前为历史 |
| horizon | int | 预测月数，1~12 |
| lookback | int | 展示回看月数 |
| confZ | float | 区间 z 值：1.28≈80% / 1.645≈90% / 1.96≈95% |
| forceModel | string? | 指定模型 id 跳过自动择优（`naive/ma3/ses/damped/drift`），默认 null 自动择优 |
| history | array? | 前端已查到的历史可带上避免重复查库，为空则后端自查 |

响应体：

```json
{
  "unit": "元/MWh",
  "model": "naive",
  "modelName": "持平(naive)",
  "backtestMae": 34.94,
  "scores": { "naive": 34.94, "ma3": 52.13, "ses": 42.45, "damped": 42.99, "drift": 45.07 },
  "sigma": 49.20,
  "history": [ { "ym": "2025-12", "price": 380.74, "isForecast": false, "low": null, "high": null } ],
  "forecast": [ { "ym": "2026-06", "price": 310.72, "isForecast": true, "low": 247.74, "high": 373.70 } ]
}
```

> `scores` 返回全部候选模型的回测 MAE，供前端展示与审计（说明为什么选了这个模型）。

---

## 5. 预测算法与公式（核心）

数据点少（有效样本约 12 个月），且价格受政策与水电汛枯节律强烈影响，复杂模型（ARIMA/Prophet）
在这种短序列上极易过拟合。v2 采用**「有效样本裁剪 + 季节分解为主、多模型回测择优兜底」**的透明做法。

### 5.0 有效样本裁剪（`trim_effective`）

湖南 2025-06 之前为旧定价规则数据（价格长期稳定在 452 附近），与当前市场机制不同，
其趋势与波动无参考价值。故只取 `EFFECTIVE_START`（默认 `2025-06`）及以后的数据建模。
起始月可配置；若晚于全部数据则兜底保留最后 `MIN_HISTORY` 个点。

### 5.1 季节分解模型（主模型，`_seasonal_project`）

核心公式：

```
去季节化:   deseason[t] = price[t] / 季节因子[month(t)]
当前水平:   level = 近 LEVEL_LOOKBACK 月 deseason 的指数加权平均（近月权重高）
水平斜率:   slope = 近 LEVEL_TREND_K 月 deseason 的线性斜率
预测:       水平_h = level + slope × Σ φ^j   (j=1..h，φ=LEVEL_TREND_PHI 强阻尼)
           ŷ_h   = clip( 水平_h × 季节因子[目标月] )
```

- **季节因子**是显式领域先验（12 个月样本不足以从数据统计季节性），
  依据《2026年电力市场供需分析预测（春季）报告·湖南》校准：
  6 月电量盈余最大、5 月上旬弃电最多 → 汛期（4-6 月）价格最低；
  7-8 月度夏负荷创新高、电力紧平衡 → 回升；枯水期（10-3 月）来水少 → 偏高。
  因子在 `SEASONAL_RAW` 配置，使用时自动归一化到均值 1.0，可随新报告调整。
- **水平**用去季节化后的近期加权均值，既承认政策性下移，又不被单月低点（如 5 月）带偏。
- **趋势**强阻尼（φ=0.5），防止把政策性一次下移当成永久趋势无限外推。

> 效果：预测随水电汛枯节律**先降后升**（6 月低点 → 汛期结束回升 → 枯水期高点），
> 而非 v1 那样单调走低。这与官方供需报告的大趋势一致。

### 5.2 模型择优（季节优先 + 回测兜底，`select_model`）

候选模型：`seasonal`（季节分解）、`naive`、`ma3`、`ses`、`drift`。
对每个模型做滚动一步回测得 MAE。择优规则：

```
best = argmin MAE
若 seasonal 的 MAE ≤ best 的 MAE × (1 + SEASONAL_TOLERANCE)：   # 默认容忍 20%
    采用 seasonal（业务含季节性，优先反映汛枯节律）
否则采用 best
数据不足（< MIN_HISTORY）时退化为 naive
```

> 为什么季节优先：naive 常有最低 MAE，但它把 5 月的季节性低点当常态、无法体现回升，
> 对交易员误导。只要季节模型精度不明显更差（容忍度内），就优先采用它。
> `scores` 返回全部候选 MAE，可审计"为什么选季节分解"。

### 5.3 置信区间

区间宽度来自回测残差标准差 `sigma`，随步长按 √h 展宽：

```
band_h = confZ × sigma × √h
low_h  = clip(ŷ_h − band_h)
high_h = clip(ŷ_h + band_h)
```

confZ 默认 1.28（80%）。区间承载不确定性：预测越远越宽。

## 6. 前端渲染要点

1. **一条折线两种样式**：历史段实线（灰 `#64748b`）、预测段虚线（蓝 `#2563eb`，`stroke-dasharray`），在基准月处用竖虚线分界并标「预测起点」。
2. **置信区间**：预测段 `low`~`high` 用浅蓝 `#bfdbfe` 半透明多边形填充，从历史末点接出保证连续。
3. **表格**：历史行打「历史」灰标签、预测行打「预测」蓝标签；预测行显示区间上下限；环比涨绿跌红（电力行业习惯，可按贵司规范调整配色）。
4. **随参数刷新**：`lookback / horizon / confZ` 变化时重新请求 `POST /forecast` 并重绘。

---

## 7. 字段字典（预测明细表）

| 字段 | 类型 | 单位 | 来源 | 边界/校验 |
|----|----|----|----|--------|
| ym | string | — | — | YYYY-MM |
| price | float | 元/MWh | 5.3 | clip 到 [FLOOR, CAP] |
| isForecast | bool | — | — | true=预测行 |
| low | float? | 元/MWh | 5.4 | 历史行为 null；≥ FLOOR |
| high | float? | 元/MWh | 5.4 | 历史行为 null；≤ CAP |

---

## 8. 边界与异常规则汇总

1. **历史不足**：少于 4 个月无法可靠择优，退化为 naive 持平，UI 可提示「历史样本少，预测仅供参考」。
2. **价格截断**：预测值及区间超出 `[PRICE_FLOOR, PRICE_CAP]` 时截断，防止异常外推（如漂移模型把价预测成负数）。
3. **区间展宽**：区间必须随步长单调展宽（下限递减、上限递增），体现远期不确定性更大。
4. **基准月无实际值**：`asOfMonth` 当月通常尚无结算均价，历史序列取其**之前**的月份，预测从 `asOfMonth` 起（见原型 `computeForecast`）。若贵司当月已有值，按实际口径调整即可。
5. **口径一致性**：历史与预测须同口径（同为全省 或 同为某场站），跨口径拼接会失真。

---

## 9. 实施建议

- **算法落后端**（`algorithm.py` 纯函数），前端微调走等价本地预算，展示/保存以后端为准，杜绝公式漂移。
- `LOOKBACK_MONTHS / HORIZON_MONTHS / PRICE_FLOOR / PRICE_CAP / CONF_Z / BACKTEST_K` 及各模型参数抽成**可配置项**，按地区市场规则调整，不写死。
- **数据越多模型越可靠**：目前约 17 个月。建议持续滚动补充月度实际值；累计满两个完整年度后，可在候选集中加入**季节性模型**（如 Holt-Winters）并纳入回测比较，若季节模型 MAE 更低会自动被选中。
- **二期可选**：把现货预测均价作为中长期价的**领先解释变量**（现货领先中长期约 1~2 月的关系值得验证），从单序列外推升级为带解释变量的模型；同样通过回测决定是否采用。
- 关注**政策事件标注**：中长期价的阶跃多由政策文件驱动（如湘发改价调〔2026〕31号差价结算细则）。可在图上叠加政策事件竖线标注，帮助交易员理解拐点成因。

---

*附：交互原型见 `中长期电价预测原型.html`（零依赖，浏览器直接打开），可对照本文档逐点验证图表与表格表现。*
