# 新能源电力交易辅助决策系统 - 页面级别数据需求文档

## 版本信息

| 版本 | 日期 | 作者 | 说明 |
|------|------|------|------|
| V2.0 | 2026-07-13 | 产品经理 | 完整版，覆盖所有页面所有组件 |

---

## 一、概述

### 1.1 文档目的

本文档按照**页面级别**和**组件级别**，详细定义新能源电力交易辅助决策系统中每个业务展示页面的每个组件所需的数据来源、字段映射、查询方式和计算逻辑，为后端开发提供明确的数据需求依据。

### 1.2 适用范围

本文档适用于系统所有模块的后端开发，包括：
- 总览分析模块（市场行情、复盘看板）
- 预测信息模块（功率预测精度分析、电价预测、负荷预测、气象信息看板）
- 中长期交易模块（中长期持仓、中长期辅助决策）
- 现货交易模块（现货市场、价格日历、现货辅助决策）
- 后台数据维护模块（场站信息管理、合约管理、SCADA实时数据管理等）
- 系统管理模块（用户管理、数据字典、操作日志等）

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| 页面级别 | 指系统中的每个独立页面 |
| 组件级别 | 指页面中的每个独立展示单元（筛选栏、指标卡、图表、表格等） |
| 数据映射 | 组件与数据源之间的对应关系 |
| 查询方式 | 获取数据的SQL查询逻辑或API接口 |
| 计算逻辑 | 数据的聚合、转换、计算规则 |
| 数据流向 | 数据在系统中的输入、处理、输出路径 |

---

## 二、总览分析模块

### 2.1 市场行情页面

**页面路径**: `总览分析/市场行情/index.html`
**页面功能**: 全省电力市场行情数据概览

#### 2.1.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 数据周期下拉框 | period | VARCHAR(10) | month | 日/月 | 用户选择 |
| 日期输入框(日) | date_day | DATE | 当前日期 | 日维度查询日期 | 用户输入 |
| 月份输入框(月) | date_month | VARCHAR(7) | 当前月份 | 月维度查询日期，格式YYYY-MM | 用户输入 |
| 查询按钮 | - | - | - | 触发数据刷新 | 用户操作 |
| 导出按钮 | - | - | - | 导出当前筛选数据 | 用户操作 |
| 数据更新时间 | update_time | DATETIME | - | 显示数据最后更新时间 | 系统自动 |

**联动逻辑**:
- 选择"日"时，显示日期输入框，隐藏月份输入框
- 选择"月"时，显示月份输入框，隐藏日期输入框

**API接口**: `GET /api/market/overview`

#### 2.1.2 宏观数据卡片组件

| 组件名称 | 显示字段 | 单位 | 数据来源表 | 计算逻辑 | 筛选条件 |
|----------|----------|------|------------|----------|----------|
| 本项目装机容量 | installed_capacity_mw | MW | station_info | SUM(installed_capacity_mw) | status=1(启用) |
| 同比增长率 | - | % | station_info | 本期/上期 × 100% - 100% | 同期对比 |
| 本月利用小时 | - | h | scada_data + station_info | 本月总发电量 / 总装机容量 | data_date在本月 |
| 环比增长率 | - | % | scada_data | 本期/上期 × 100% - 100% | 上月对比 |
| 本月发电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date在本月，turbine_no='汇总' |
| 场站综合电价 | - | 元/MWh | contract_info + spot_data | 加权平均计算 | 根据筛选周期 |
| 综合电价明细 | - | - | contract_info + spot_data | 中长期均价 + 现货均价 | - |
| 年度累计发电小时数 | - | h | scada_data + station_info | 年度总发电量 / 总装机容量 | data_date在本年 |
| 年度累计发电电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date在本年，turbine_no='汇总' |

**综合电价计算逻辑**:
```
综合电价 = (中长期合约电量 × 合约电价 + 现货电量 × 现货电价) / (中长期合约电量 + 现货电量)
```

**API接口**: `GET /api/market/macro-data`

#### 2.1.3 湖南省电源装机结构图表

**图表类型**: 饼图

| 图表元素 | 数据来源表 | 使用字段 | 聚合方式 | 说明 |
|----------|------------|----------|----------|------|
| 分类标签 | station_info | station_type | - | 场站类型（风电/光伏/水电等） |
| 数值 | station_info | installed_capacity_mw | SUM(installed_capacity_mw) | 装机容量 |
| 分组 | - | - | GROUP BY station_type | 按场站类型分组 |
| 图例 | - | - | 数据字典station-type | 类型名称映射 |

**API接口**: `GET /api/station/capacity-structure`

#### 2.1.4 各类交易价格汇总图表及表格

**图表类型**: 柱状图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | - | - | 交易类型（固定枚举值） |
| Y轴-市场均价 | contract_info + spot_data | price / day_ahead_price | 市场平均电价 |
| Y轴-场站均价 | contract_info + spot_data | price / day_ahead_price | 场站平均电价 |

**表格数据**:

| 表格列 | 数据来源表 | 计算逻辑 | 说明 |
|--------|------------|----------|------|
| 交易类型 | - | 固定枚举值 | 中长期集中、中长期双边、绿电交易、现货日前、现货实时 |
| 市场均价 | contract_info + spot_data | AVG(对应类型电价) | 市场平均电价 |
| 场站均价 | contract_info + spot_data | AVG(场站对应类型电价) | 场站平均电价 |
| 成交量 | contract_info + spot_data | SUM(电量) | 对应类型总电量 |

**数据映射规则**:

| 交易类型 | 数据来源表 | 查询条件 |
|----------|------------|----------|
| 中长期集中 | contract_info | contract_type='月度集中竞价合约' OR contract_type='年度集中竞价合约' |
| 中长期双边 | contract_info | contract_type='年度双边协商合约' OR contract_type='月度双边协商合约' OR contract_type='季度双边协商合约' |
| 绿电交易 | contract_info | contract_type='绿电交易' |
| 现货日前 | spot_data | 日前出清数据 |
| 现货实时 | spot_data | 实时出清数据 |

**API接口**: `GET /api/market/price-summary`

#### 2.1.5 供需情况图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | Y轴 | 说明 |
|----------|------------|----------|-----|------|
| X轴 | spot_data | trade_period | - | 00:00-24:00，按小时分组 |
| 负荷 | prediction_data | predicted_value | 左Y轴 | 负荷预测值，prediction_type='负荷预测' |
| 新能源出力 | scada_data | actual_power_mw | 右Y轴 | 实际上网功率，按小时聚合 |

**计算逻辑**:
- 负荷：按小时分组，AVG(predicted_value)
- 新能源出力：按小时分组，SUM(actual_power_mw) × 0.25

**数据筛选**: 根据筛选周期

**API接口**: `GET /api/market/supply-demand`

#### 2.1.6 市场价格曲线图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | contract_info + spot_data | 时间维度 | 按日期或月份分组 |
| Y轴-中长期价格 | contract_info | price | 合约电价，按时间聚合 |
| Y轴-现货价格 | spot_data | day_ahead_price | 日前出清电价，按时间聚合 |

**计算逻辑**:
- 日维度：按日期分组，AVG(price) / AVG(day_ahead_price)
- 月维度：按月份分组，AVG(price) / AVG(day_ahead_price)

**API接口**: `GET /api/market/price-curve`

#### 2.1.7 出力预测偏差率图表

**图表类型**: 折线图/柱状图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | data_date | 日期 |
| Y轴-偏差率 | scada_data | - | \|predicted_power_mw - actual_power_mw\| / actual_power_mw × 100% |

**计算逻辑**:
```
日偏差率 = AVG(\|predicted_power_mw - actual_power_mw\| / actual_power_mw × 100%) 
WHERE turbine_no='汇总' AND data_date=当日
```

**API接口**: `GET /api/market/forecast-deviation`

#### 2.1.8 交易出清电量统计图表

**图表类型**: 柱状图/堆叠图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | spot_data | trade_date | 日期 |
| Y轴-日前出清电量 | spot_data | day_ahead_volume | 日前出清电量 |
| Y轴-实时出清电量 | spot_data | real_time_volume | 实时出清电量 |
| 分组 | spot_data | power_type | 电源类型 |

**计算逻辑**:
- 按日期分组，SUM(day_ahead_volume) / SUM(real_time_volume)
- 按电源类型堆叠显示

**API接口**: `GET /api/market/clearing-volume`

---

### 2.2 复盘看板页面

**页面路径**: `总览分析/复盘看板/index.html`
**页面功能**: 交易结果分析与结算对比

#### 2.2.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 开始日期 | start_date | DATE | 本月第一天 | 查询起始日期 | 用户输入 |
| 结束日期 | end_date | DATE | 当前日期 | 查询结束日期 | 用户输入 |
| 查询按钮 | - | - | - | 触发数据刷新 | 用户操作 |
| 导出按钮 | - | - | - | 导出结算数据 | 用户操作 |

**API接口**: `GET /api/review/board`

#### 2.2.2 数据卡片组件

| 组件名称 | 显示字段 | 单位 | 数据来源表 | 计算逻辑 | 筛选条件 |
|----------|----------|------|------------|----------|----------|
| 中长期合约-电量 | trade_volume_mwh | MWh | settlement_data | SUM(trade_volume_mwh) | settlement_type='中长期结算' |
| 中长期合约-电费 | settlement_amount | 万元 | settlement_data | SUM(settlement_amount) | settlement_type='中长期结算' |
| 中长期合约-电价 | settlement_price | 元/MWh | settlement_data | AVG(settlement_price) | settlement_type='中长期结算' |
| 日前偏差-电量 | trade_volume_mwh | MWh | settlement_data | SUM(trade_volume_mwh) | settlement_type='日前结算' |
| 日前偏差-电费 | settlement_amount | 万元 | settlement_data | SUM(settlement_amount) | settlement_type='日前结算' |
| 日前偏差-电价 | settlement_price | 元/MWh | settlement_data | AVG(settlement_price) | settlement_type='日前结算' |
| 实时偏差-电量 | trade_volume_mwh | MWh | settlement_data | SUM(trade_volume_mwh) | settlement_type='实时结算' |
| 实时偏差-电费 | settlement_amount | 万元 | settlement_data | SUM(settlement_amount) | settlement_type='实时结算' |
| 实时偏差-电价 | settlement_price | 元/MWh | settlement_data | AVG(settlement_price) | settlement_type='实时结算' |
| 发电机组超额回收 | settlement_amount | 万元 | settlement_data | SUM(settlement_amount) | settlement_type='偏差结算' |

**筛选条件**: settlement_month BETWEEN start_date AND end_date

**API接口**: `GET /api/review/stats`

#### 2.2.3 结算单对比表格组件

| 表格列 | 数据来源表 | 计算逻辑 | 说明 |
|--------|------------|----------|------|
| 项目 | - | 固定枚举值 | 中长期合约电量、日前偏差电量、实时偏差电量、总电费、超额回收金额 |
| 本期值 | settlement_data | 当前查询周期的聚合值 | 根据项目类型计算 |
| 上期值 | settlement_data | 上一周期的聚合值 | 同期对比，如本月对比上月 |
| 差值 | - | 本期值 - 上期值 | 计算差值 |
| 变化率 | - | (本期值 - 上期值) / 上期值 × 100% | 计算变化率 |
| 备注 | - | 系统根据变化率自动生成 | 如"合约量增加"、"偏差率降低"等 |

**表格数据计算规则**:

| 项目 | 计算字段 | 聚合方式 | 结算类型筛选 |
|------|----------|----------|--------------|
| 中长期合约电量 | trade_volume_mwh | SUM | settlement_type='中长期结算' |
| 日前偏差电量 | trade_volume_mwh | SUM | settlement_type='日前结算' |
| 实时偏差电量 | trade_volume_mwh | SUM | settlement_type='实时结算' |
| 总电费 | settlement_amount | SUM | 所有结算类型 |
| 超额回收金额 | settlement_amount | SUM | settlement_type='偏差结算' |

**变化率颜色规则**:
- 正值（增长）：绿色
- 负值（下降）：蓝色

**API接口**: `GET /api/review/compare`

#### 2.2.4 图表日期选择器组件

| 组件名称 | 字段名称 | 数据类型 | 说明 | 数据来源 |
|----------|----------|----------|------|----------|
| 日期下拉框 | chart_date | DATE | 选择查看的日期 | settlement_data表（查询范围内的日期列表） |

**联动逻辑**:
- 查询按钮点击后，动态生成查询范围内的日期选项
- 默认选中开始日期
- 日期变更时，更新当日交易电量曲线和电价曲线

**API接口**: `GET /api/review/date-options`

#### 2.2.5 当日交易电量曲线图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | spot_data | trade_period | 00:00-24:00，按小时分组 |
| Y轴-实际执行电量 | spot_data | real_time_volume | 实时出清电量，按小时聚合 |
| Y轴-日前出清电量(预测) | prediction_data | predicted_value | 预测电量，prediction_type='功率预测' |

**计算逻辑**:
- 实际执行电量：按小时分组，SUM(real_time_volume)
- 日前出清电量(预测)：按小时分组，取功率预测值

**数据筛选**: trade_date = 选中日期

**API接口**: `GET /api/review/volume-curve?date={chart_date}`

#### 2.2.6 当日交易电价曲线图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | spot_data | trade_period | 00:00-24:00，按小时分组 |
| Y轴-实际出清电价 | spot_data | real_time_price | 实时出清电价，按小时平均 |
| Y轴-日前出清电价(预测) | prediction_data | predicted_value | 预测电价，prediction_type='电价预测' |

**计算逻辑**:
- 实际出清电价：按小时分组，AVG(real_time_price)
- 日前出清电价(预测)：按小时分组，取电价预测值

**数据筛选**: trade_date = 选中日期

**API接口**: `GET /api/review/price-curve?date={chart_date}`

---

## 三、预测信息模块

### 3.1 功率预测精度分析页面

**页面路径**: `预测信息/功率预测精准分析/index.html`
**页面功能**: 功率预测精度统计与分析

#### 3.1.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 场站下拉框 | station_id | VARCHAR(32) | 第一个场站 | 场站名称 | station_info表 |
| 开始时间 | start_date | DATE | 本月第一天 | 查询起始日期 | 用户输入 |
| 结束时间 | end_date | DATE | 当前日期 | 查询结束日期 | 用户输入 |
| 气象数据源 | data_source | VARCHAR(50) | all | ECMWF/GFS/NAM/本地观测 | 用户选择 |
| 预测精度 | accuracy_level | VARCHAR(10) | 1h | 15min/1h/1d/3d/7d | 用户选择 |
| 查询按钮 | - | - | - | 触发数据刷新 | 用户操作 |
| 查询状态 | query_status | VARCHAR(50) | - | 显示查询进度和结果 | 系统反馈 |

**API接口**: `GET /api/prediction/power/accuracy`

#### 3.1.2 1小时间隔预测对比图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | time_slot | 按小时聚合（00:00-24:00） |
| Y轴-实际功率 | scada_data | actual_power_mw | 实际上网功率，按小时平均 |
| Y轴-预测功率 | scada_data | predicted_power_mw | 预测上网功率，按小时平均 |

**计算逻辑**:
```sql
SELECT time_slot, 
       AVG(actual_power_mw) AS actual_power, 
       AVG(predicted_power_mw) AS predicted_power
FROM scada_data
WHERE data_date BETWEEN :start_date AND :end_date
  AND turbine_no = '汇总'
GROUP BY time_slot
ORDER BY time_slot
```

**API接口**: `GET /api/prediction/power/hourly-compare`

#### 3.1.3 15分钟间隔预测对比图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | time_slot | 15分钟间隔（96点/日） |
| Y轴-实际功率 | scada_data | actual_power_mw | 实际上网功率 |
| Y轴-预测功率 | scada_data | predicted_power_mw | 预测上网功率 |

**数据筛选**: data_date=查询日期（日维度），turbine_no='汇总'

**交互特性**:
- 支持数据缩放拖拽
- 显示"96点/日"标签

**API接口**: `GET /api/prediction/power/15min-compare`

#### 3.1.4 功率预测误差分析指标

| 指标名称 | 数据来源表 | 计算公式 | 单位 | 说明 |
|----------|------------|----------|------|------|
| 平均绝对误差 (MAE) | scada_data | Σ\|predicted_power_mw - actual_power_mw\| / n | MW | 平均绝对偏差 |
| 均方根误差 (RMSE) | scada_data | √(Σ(predicted_power_mw - actual_power_mw)² / n) | MW | 均方根偏差 |
| 均方根百分比误差 (MAPE) | scada_data | Σ(\|predicted_power_mw - actual_power_mw\| / \|actual_power_mw\|) / n × 100% | % | 平均绝对百分比误差 |

**数据筛选**: data_date在查询范围内，turbine_no='汇总'

**API接口**: `GET /api/prediction/power/error-metrics`

#### 3.1.5 误差分布图表

**图表类型**: 直方图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | 偏差区间 | 按偏差大小分组（如-5~-4, -4~-3, ..., 3~4, 4~5） |
| Y轴 | scada_data | 频次 | 各偏差区间的数据条数 |

**计算逻辑**:
```
偏差 = predicted_power_mw - actual_power_mw
按偏差区间分组，统计每组记录数
```

**API接口**: `GET /api/prediction/power/error-distribution`

#### 3.1.6 近7天功率预测准确率图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | data_date | 近7天日期 |
| Y轴-准确率 | scada_data | - | 1 - MAPE/100 |

**计算逻辑**:
```sql
SELECT data_date,
       1 - AVG(ABS(predicted_power_mw - actual_power_mw) / ABS(actual_power_mw)) AS accuracy
FROM scada_data
WHERE data_date >= DATE_SUB(CURRENT_DATE, INTERVAL 7 DAY)
  AND turbine_no = '汇总'
GROUP BY data_date
ORDER BY data_date
```

**API接口**: `GET /api/prediction/power/accuracy-7days`

#### 3.1.7 近30天功率预测准确率趋势图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | scada_data | data_date | 近30天日期 |
| Y轴-准确率 | scada_data | - | 1 - MAPE/100 |

**API接口**: `GET /api/prediction/power/accuracy-30days`

#### 3.1.8 功率预测数据表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时间 | scada_data | data_date + time_slot | 组合显示 | 格式：YYYY-MM-DD HH:mm |
| 实际功率 (MW) | scada_data | actual_power_mw | 直接显示 | 实际上网功率 |
| 预测功率 (MW) | scada_data | predicted_power_mw | 直接显示 | 预测上网功率 |
| 偏差 (MW) | scada_data | - | predicted_power_mw - actual_power_mw | 功率偏差 |
| 偏差率 (%) | scada_data | - | \|predicted_power_mw - actual_power_mw\| / actual_power_mw × 100% | 偏差率 |

**数据筛选**: data_date在查询范围内，turbine_no='汇总'

**分页参数**:
- pageNum: 当前页码
- pageSize: 每页条数（默认10条）

**API接口**: `GET /api/prediction/power/data-list`

---

### 3.2 电价预测页面

**页面路径**: `预测信息/电价预测/index.html`
**页面功能**: 短期及中长期电价预测分析

#### 3.2.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 训练节点 | training_node | VARCHAR(50) | 全部 | 算法训练节点 | algorithm_model表 |
| 预测日期 | forecast_date | DATE | 当前日期 | 触发预测的日期 | 用户输入 |
| 查询日期 | query_date | DATE | - | 查询历史预测结果的日期 | 用户输入 |
| 日前预测按钮 | - | - | - | 触发电价预测 | 用户操作 |
| 查询按钮 | - | - | - | 查询历史预测结果 | 用户操作 |

**API接口**: `POST /api/prediction/price/day-ahead`（预测）, `GET /api/prediction/price/history`（查询）

#### 3.2.2 日前现货价格预测图表及表格

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | prediction_data | time_slot | 15分钟间隔（96点/日） |
| Y轴-预测电价 | prediction_data | predicted_value | 电价预测值 |
| Y轴-置信区间下限 | prediction_data | lower_bound | 95%置信区间下限 |
| Y轴-置信区间上限 | prediction_data | upper_bound | 95%置信区间上限 |

**数据筛选**: prediction_date=预测日期，prediction_type='电价预测'

**表格数据**:

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时段(15分钟) | prediction_data | time_slot | 直接显示 | 15分钟时段 |
| 日前预测价(元/MWh) | prediction_data | predicted_value | 直接显示 | 电价预测值 |
| 置信区间(95%) | prediction_data | lower_bound ~ upper_bound | 组合显示 | 置信区间范围 |
| 偏差率(%) | prediction_data | - | 与实际电价对比计算 | 预测偏差率 |

**API接口**: `GET /api/prediction/price/day-ahead-detail`

#### 3.2.3 未来7天现货价格预测图表及表格

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | prediction_data | prediction_date | 未来7天日期 |
| Y轴-最高价 | prediction_data | upper_bound | 每日最高预测电价 |
| Y轴-最低价 | prediction_data | lower_bound | 每日最低预测电价 |
| Y轴-均价 | prediction_data | predicted_value | 每日平均预测电价 |

**数据筛选**: prediction_type='电价预测'，prediction_date在未来7天

**表格数据**:

| 表格列 | 数据来源表 | 计算逻辑 | 说明 |
|--------|------------|----------|------|
| 日期 | prediction_data | prediction_date | 预测日期 |
| 最高价(元/MWh) | prediction_data | MAX(upper_bound) | 当日最高预测电价 |
| 最低价(元/MWh) | prediction_data | MIN(lower_bound) | 当日最低预测电价 |
| 均价(元/MWh) | prediction_data | AVG(predicted_value) | 当日平均预测电价 |
| 波动率(%) | prediction_data | - | (最高价 - 最低价) / 均价 × 100% | 价格波动率 |

**API接口**: `GET /api/prediction/price/week-forecast`

#### 3.2.4 中长期电价预测图表及表格

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | prediction_data | prediction_date | 按月分组 |
| Y轴-最高价 | prediction_data | upper_bound | 每月最高预测电价 |
| Y轴-最低价 | prediction_data | lower_bound | 每月最低预测电价 |
| Y轴-均价 | prediction_data | predicted_value | 每月平均预测电价 |

**数据筛选**: prediction_type='电价预测'，中长期预测数据

**表格数据**:

| 表格列 | 数据来源表 | 计算逻辑 | 说明 |
|--------|------------|----------|------|
| 月份 | prediction_data | prediction_date | 按月份分组 |
| 最高价(元/MWh) | prediction_data | MAX(upper_bound) | 当月最高预测电价 |
| 最低价(元/MWh) | prediction_data | MIN(lower_bound) | 当月最低预测电价 |
| 均价(元/MWh) | prediction_data | AVG(predicted_value) | 当月平均预测电价 |
| 波动率(%) | prediction_data | - | (最高价 - 最低价) / 均价 × 100% | 价格波动率 |

**API接口**: `GET /api/prediction/price/medium-long-term`

---

### 3.3 负荷预测页面

**页面路径**: `预测信息/负荷预测/index.html`
**页面功能**: 全省及地市电力负荷预测分析

#### 3.3.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 预测区域 | forecast_area | VARCHAR(50) | 全省 | 全省/长沙/株洲/湘潭等 | 用户选择 |
| 预测分类 | forecast_category | VARCHAR(50) | 全社会用电量 | 全社会用电量/居民生活/第一产业/第二产业/第三产业 | 用户选择 |
| 负荷方案 | load_plan | VARCHAR(100) | 当前方案 | 预测方案名称 | algorithm_model表 |
| 预测日期 | forecast_date | DATE | 当前日期 | 触发预测的日期 | 用户输入 |
| 查询日期 | query_date | DATE | 当前日期 | 查询历史预测结果的日期 | 用户输入 |
| 负荷预测按钮 | - | - | - | 触发负荷预测 | 用户操作 |
| 查询按钮 | - | - | - | 查询历史预测结果 | 用户操作 |

**API接口**: `POST /api/prediction/load/forecast`（预测）, `GET /api/prediction/load/history`（查询）

#### 3.3.2 负荷预测图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | Y轴 | 说明 |
|----------|------------|----------|-----|------|
| X轴 | prediction_data | time_slot | - | 按小时分组（00:00-24:00） |
| Y轴-预测负荷 | prediction_data | predicted_value | 左Y轴 | 负荷预测值 |
| Y轴-置信区间下限 | prediction_data | lower_bound | 左Y轴 | 95%置信区间下限 |
| Y轴-置信区间上限 | prediction_data | upper_bound | 左Y轴 | 95%置信区间上限 |
| Y轴-实际负荷 | - | - | 右Y轴 | 历史实际负荷数据（如有） |

**数据筛选**: prediction_date=预测日期，prediction_type='负荷预测'

**API接口**: `GET /api/prediction/load/chart-data`

#### 3.3.3 总用电量汇总

| 组件名称 | 数据来源表 | 计算逻辑 | 说明 |
|----------|------------|----------|------|
| 总用电量 | prediction_data | SUM(predicted_value) | 当日所有时段预测负荷之和 |

**数据筛选**: prediction_date=预测日期，prediction_type='负荷预测'

**API接口**: `GET /api/prediction/load/total`

#### 3.3.4 负荷预测数据表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时间 | prediction_data | time_slot | 直接显示 | 预测时段 |
| 预测负荷(MW) | prediction_data | predicted_value | 直接显示 | 负荷预测值 |
| 实际负荷(MW) | - | - | - | 历史实际负荷（如有） |
| 置信区间(MW) | prediction_data | lower_bound ~ upper_bound | 组合显示 | 95%置信区间 |
| 偏差率(%) | prediction_data | - | \|预测负荷 - 实际负荷\| / 实际负荷 × 100% | 偏差率 |

**API接口**: `GET /api/prediction/load/data-list`

---

### 3.4 气象信息看板页面

**页面路径**: `预测信息/气象信息看板/index.html`
**页面功能**: 气象数据展示与分析，包含省份地图热力图、场站气象数据一览、新能源出力预测等

#### 3.4.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 省份 | province | VARCHAR(50) | 湖南省 | 湖南省/广东省/江西省/湖北省/贵州省 | 用户选择 |
| 气象数据源 | data_source | VARCHAR(50) | ECMWF | ECMWF/国家气象中心(NMC)/GRIB2/WRF模式 | 用户选择 |
| 预测时长 | forecast_duration | VARCHAR(10) | 1d | 1h/1d/3d/1w | 用户选择 |
| 气象类型 | weather_type | VARCHAR(20) | 全部 | 全部/风速/辐照度/温度/湿度 | 用户选择 |
| 查询日期 | data_date | DATE | 当前日期 | 查询日期 | 用户输入 |
| 查询数据按钮 | - | - | - | 触发数据查询 | 用户操作 |
| 刷新按钮 | - | - | - | 刷新数据 | 用户操作 |
| 数据来源显示 | current_source | VARCHAR(50) | - | 当前数据源名称 | 系统反馈 |
| 最后更新时间 | last_update_time | DATETIME | - | 数据最后更新时间 | 系统自动 |

**API接口**: `GET /api/weather/dashboard`

#### 3.4.2 气象要素热力地图组件

**图表类型**: ECharts地图热力图

**地图数据源**: 预加载的GeoJSON文件（前端静态资源）

| 省份 | GeoJSON文件 | 地图名称 | 中心点坐标 |
|------|------------|----------|------------|
| 湖南省 | hunan.json | hunan | [111.7, 27.5] |
| 广东省 | guangdong.json | guangdong | [113.5, 23.5] |
| 江西省 | jiangxi.json | jiangxi | [115.9, 27.0] |
| 湖北省 | hubei.json | hubei | [112.4, 31.0] |
| 贵州省 | guizhou.json | guizhou | [106.7, 26.5] |

**热力图数据**:

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| 热力值 | weather_data + station_info | wind_speed | 各城市平均风速 |
| 城市名称 | GeoJSON | properties.name | 城市名称 |

**计算逻辑**:
```sql
SELECT station_info.city, AVG(weather_data.wind_speed) AS avg_wind_speed
FROM weather_data
JOIN station_info ON weather_data.station_name = station_info.station_name
WHERE weather_data.data_time BETWEEN :start_time AND :end_time
  AND station_info.province = :province
GROUP BY station_info.city
```

**地图交互**:
- 点击城市可钻取到城市级别，显示城市内场站分布
- 点击场站可查看场站详细气象数据和出力预测
- 显示钻取路径（湖南省 › 长沙市）
- 提供"返回全省"按钮

**API接口**: `GET /api/weather/heatmap-data`

#### 3.4.3 场站气象要素一览表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 场站名称 | station_info | station_name | 直接显示 | 场站名称 |
| 类型 | station_info | station_type | 直接显示 | 场站类型（风电/光伏/风光互补） |
| 实时风速(m/s) | weather_data | wind_speed | 获取最新时刻数据 | 最新风速 |
| 预测风速(m/s) | prediction_data | predicted_value | 获取最新预测值 | 风速预测值 |
| 实时辐照(W/m²) | weather_data | irradiance | 获取最新时刻数据 | 最新辐照度 |
| 预测辐照(W/m²) | prediction_data | predicted_value | 获取最新预测值 | 辐照度预测值 |

**数据筛选**: 
- station_info.province = 选中省份
- 若已钻取到城市，则筛选 station_info.city = 选中城市

**API接口**: `GET /api/weather/station-overview`

#### 3.4.4 未来三个月气象预测表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 预测月份 | prediction_data | prediction_date | 按月分组 | 预测月份 |
| 平均风速(m/s) | prediction_data | predicted_value | 月度平均 | 风速预测值，prediction_type='气象预测-风速' |
| 平均辐照(W/m²) | prediction_data | predicted_value | 月度平均 | 辐照度预测值，prediction_type='气象预测-辐照' |
| 平均温度(°C) | prediction_data | predicted_value | 月度平均 | 温度预测值，prediction_type='气象预测-温度' |
| 平均湿度(%) | prediction_data | predicted_value | 月度平均 | 湿度预测值，prediction_type='气象预测-湿度' |
| 预计发电量(MWh) | station_info + prediction_data | - | 装机容量 × 利用小时数 × 预测系数 | 预计发电量 |

**数据筛选**: 
- prediction_type='气象预测'
- prediction_date在未来3个月内

**API接口**: `GET /api/weather/quarter-forecast`

#### 3.4.5 电量统计卡片组件

| 组件名称 | 显示字段 | 单位 | 数据来源表 | 计算逻辑 | 筛选条件 |
|----------|----------|------|------------|----------|----------|
| 日累计电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date=查询日期，turbine_no='汇总' |
| 日计划完成 | - | - | scada_data + system_config | 实际/计划 × 100% | - |
| 月累计电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date在本月，turbine_no='汇总' |
| 月计划完成 | - | - | scada_data + system_config | 实际/计划 × 100% | - |
| 季度累计电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date在本季度，turbine_no='汇总' |
| 季度计划完成 | - | - | scada_data + system_config | 实际/计划 × 100% | - |
| 年度累计电量 | actual_power_mw | MWh | scada_data | SUM(actual_power_mw) × 0.25 | data_date在本年，turbine_no='汇总' |
| 年度计划完成 | - | - | scada_data + system_config | 实际/计划 × 100% | - |

**计划完成计算**:
```
计划目标电量 = 装机容量 × 年设计利用小时数 × 时间进度比例
计划完成率 = 实际累计电量 / 计划目标电量 × 100%
```

**API接口**: `GET /api/weather/electricity-stats`

#### 3.4.6 场站选择联动组件

| 组件名称 | 字段名称 | 数据类型 | 说明 | 数据来源 |
|----------|----------|----------|------|----------|
| 场站选择 | station_id | VARCHAR(32) | 场站ID | station_info表 |

**联动逻辑**:
- 选择场站后，更新电量统计卡片、新能源出力预测曲线、全省气象要素预测图表为该场站数据
- 选择"全部场站"时，恢复为全省数据

**API接口**: `GET /api/station/list`

#### 3.4.7 选中场站指示器组件

| 组件名称 | 显示字段 | 数据来源表 | 使用字段 |
|----------|----------|------------|----------|
| 当前选中场站 | station_name | station_info | station_name |
| 场站元信息 | - | station_info | station_type, installed_capacity_mw, city |

**显示格式**: `[场站名称] - [类型] [装机容量]MW - [城市]`

#### 3.4.8 新能源出力预测曲线图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | prediction_data | time_slot | 按4小时分组（00:00, 04:00, 08:00, 12:00, 16:00, 20:00, 24:00） |
| Y轴-风电预测 | prediction_data | predicted_value | 风电功率预测值，prediction_type='功率预测'，场站类型='风电' |
| Y轴-风电实时 | scada_data | actual_power_mw | 风电实际上网功率，turbine_no='汇总'，场站类型='风电' |
| Y轴-光伏预测 | prediction_data | predicted_value | 光伏功率预测值，prediction_type='功率预测'，场站类型='光伏' |
| Y轴-光伏实时 | scada_data | actual_power_mw | 光伏实际上网功率，turbine_no='汇总'，场站类型='光伏' |

**计算逻辑**:
- 全省级别：SUM(所有场站的预测/实际功率)
- 城市级别：SUM(城市内所有场站的预测/实际功率)
- 场站级别：单场站的预测/实际功率

**API接口**: `GET /api/weather/power-forecast-curve`

#### 3.4.9 全省气象要素预测图表

**图表类型**: 双Y轴折线图

| 图表元素 | 数据来源表 | 使用字段 | Y轴 | 说明 |
|----------|------------|----------|-----|------|
| X轴 | weather_data | data_time | - | 按4小时分组 |
| 实时风速 | weather_data | wind_speed | 左Y轴 | 实际风速 |
| 风速预测 | prediction_data | predicted_value | 左Y轴 | 风速预测值 |
| 实时辐照 | weather_data | irradiance | 右Y轴 | 实际辐照度 |
| 辐照预测 | prediction_data | predicted_value | 右Y轴 | 辐照度预测值 |

**API接口**: `GET /api/weather/element-forecast`

---

## 四、中长期交易模块

### 4.1 中长期持仓页面

**页面路径**: `中长期交易/中长期持仓/index.html`
**页面功能**: 合约持仓情况概览

#### 4.1.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 年份 | year | INT | 当前年份 | 合约年度 | 用户选择 |
| 场站名称 | station_id | VARCHAR(32) | all | 场站名称 | station_info表 |
| 交易方式 | trade_category | VARCHAR(50) | all | 交易品类（多选） | 数据字典trade-category |
| 查询按钮 | - | - | - | 触发数据查询 | 用户操作 |

**API接口**: `GET /api/contract/position`

#### 4.1.2 数据卡片组件

| 组件名称 | 显示字段 | 单位 | 数据来源表 | 计算逻辑 | 筛选条件 |
|----------|----------|------|------------|----------|----------|
| 签约合同总数 | id | 份 | contract_info | COUNT(id) | year=筛选年份，station_id=筛选场站，deleted_flag=0 |
| 签约合同总电量 | volume_ratio | 万 kWh | contract_info | SUM(volume_ratio) × 装机容量 × 年利用小时 / 10000 | year=筛选年份，station_id=筛选场站，deleted_flag=0 |
| 签约合同总价 | - | 万元 | contract_info | SUM(volume_ratio × price) × 装机容量 × 年利用小时 / 100000000 | year=筛选年份，station_id=筛选场站，deleted_flag=0 |
| 累计交易电量限额 | - | 万 kWh | system_config | config_value (config_code='annual_trade_limit') | - |
| 净合约电量 | volume_ratio | 万 kWh | contract_info | SUM(多头合约电量) - SUM(空头合约电量) | year=筛选年份，station_id=筛选场站，deleted_flag=0 |

**电量换算说明**:
- volume_ratio为电量占比(%)，需乘以装机容量(MW)和年利用小时数得到实际电量
- 单位换算：1 MW·h = 1000 kWh = 0.0001 万 kWh

**API接口**: `GET /api/contract/position-summary`

#### 4.1.3 合约电量情况堆叠图

**图表类型**: 堆叠柱状图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | contract_info | year | 年度 |
| Y轴 | contract_info | volume_ratio | 电量占比 |
| 分组 | contract_info | trade_category | 交易品类 |
| 聚合方式 | - | - | GROUP BY year, trade_category, SUM(volume_ratio) |

**数据筛选**: station_id=筛选场站，deleted_flag=0

**API接口**: `GET /api/contract/volume-chart`

#### 4.1.4 已签订合同完成情况占比环形图

**图表类型**: 环形图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| 已完成电量 | settlement_data | trade_volume_mwh | 已结算电量 |
| 未完成电量 | contract_info + settlement_data | - | 合约总电量 - 已完成电量 |

**计算逻辑**:
```
已完成电量 = SUM(settlement_data.trade_volume_mwh) WHERE settlement_status='已结算'
总合约电量 = SUM(contract_info.volume_ratio) × 装机容量 × 年利用小时
未完成电量 = 总合约电量 - 已完成电量
```

**数据筛选**: contract_info.year=筛选年份，settlement_data关联contract_id

**API接口**: `GET /api/contract/completion-chart`

#### 4.1.5 合同增长分析折线图

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | contract_info | create_time | 按月份分组 |
| Y轴 | contract_info | id | 累计合同数 |
| 聚合方式 | - | - | 按月份累计COUNT(id) |

**数据筛选**: station_id=筛选场站，deleted_flag=0

**API接口**: `GET /api/contract/growth-chart`

#### 4.1.6 合同占比情况饼状图

**图表类型**: 饼图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| 分类 | contract_info | trade_category | 交易品类 |
| 数值 | contract_info | volume_ratio | 电量占比 |
| 聚合方式 | - | - | GROUP BY trade_category, SUM(volume_ratio) |

**数据筛选**: year=筛选年份，station_id=筛选场站，deleted_flag=0

**API接口**: `GET /api/contract/proportion-chart`

#### 4.1.7 合同排名TOP5柱状图

**图表类型**: 柱状图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | contract_info | contract_name | 合约名称 |
| Y轴 | contract_info | volume_ratio | 电量占比 |
| 排序方式 | - | - | ORDER BY volume_ratio DESC LIMIT 5 |

**数据筛选**: year=筛选年份，station_id=筛选场站，deleted_flag=0

**API接口**: `GET /api/contract/ranking-chart`

---

### 4.2 中长期辅助决策页面

**页面路径**: `中长期交易/中长期辅助决策/index.html`
**页面功能**: 中长期交易策略分析与辅助决策

#### 4.2.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 日期 | decision_date | DATE | 当前日期 | 决策日期 | 用户输入 |
| 公司 | company_id | VARCHAR(32) | current | 所属公司 | 用户选择 |
| 策略 | strategy_type | VARCHAR(50) | default | 常规日滚动策略/保守策略/激进策略 | 用户选择 |
| 查询按钮 | - | - | - | 触发数据查询 | 用户操作 |
| 保存策略按钮 | - | - | - | 保存当前策略方案 | 用户操作 |

**API接口**: `GET /api/decision/midterm`

#### 4.2.2 策略生成模块

**三档策略卡片数据来源**:

| 策略类型 | 风险偏好 | 参考价来源 | 数据来源表 |
|----------|----------|------------|------------|
| 保守策略 | 低风险 | P30分位数 | prediction_data + spot_data |
| 中性策略 | 中等风险 | P50分位数（中位数） | prediction_data + spot_data |
| 激进策略 | 高风险 | P70分位数 | prediction_data + spot_data |

**交易方式选项**:

| 交易方式 | 说明 |
|----------|------|
| 日滚动 | 连续撮合·贴日前现货报价 |
| 月内 | 月内剩余电量交易 |
| 月度 | 月度集中竞价 |
| 多月 | 多月连续交易 |
| 年度 | 年度双边协商 |

**API接口**: `POST /api/decision/midterm/generate`

#### 4.2.3 参数微调面板组件

**面板功能**: 允许用户对中长期余量分析和申报策略进行参数微调

**微调参数**:

| 参数名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 影响范围 |
|----------|----------|----------|--------|------|----------|
| 交易类型 | trade_type | VARCHAR(20) | daily | 交易方式选择 | 全部 |
| 申报内容 | declare_content | VARCHAR(10) | 电量 | 申报电量或电价 | 全部 |
| 起始时间 | start_hour | INT | 1 | 调整起始小时 | 时段范围 |
| 终止时间 | end_hour | INT | 24 | 调整终止小时 | 时段范围 |
| 倍率 | multiplier | DECIMAL(5,2) | 1.0 | 调整倍率系数 | 电量上限 |
| 固定值 | fixed_value | DECIMAL(10,2) | 0 | 调整固定偏移量 | 电量上限 |

**数据交互逻辑**:

| 操作 | 触发条件 | 数据更新 |
|------|----------|----------|
| 修改倍率 | 用户输入倍率值并点击"修改"按钮 | 重新计算电量上限 = 时段余量 × 倍率 |
| 修改固定值 | 用户输入固定值并点击"修改"按钮 | 重新计算电量上限 = 时段余量 + 固定值 |
| 修改时段范围 | 用户选择起始/终止时间 | 只对指定时段范围应用调整 |
| 修改交易类型 | 用户选择交易类型 | 更新申报策略表格的交易类型列 |
| 修改申报内容 | 用户选择电量/电价 | 更新申报策略表格的可编辑列 |

**计算逻辑**:
```
调整后的电量上限 = 原始电量上限 × 倍率 + 固定值
```

**数据流向**:
- 参数调整 → 重新计算申报策略表格 → 更新中长期余量分析图表 → 更新中长期收益表格

**API接口**: `POST /api/decision/midterm/tune`

#### 4.2.4 中长期余量分析图表

**图表类型**: 柱状图/堆叠图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | contract_info | trading_period | 交易时段（00:00-24:00） |
| Y轴-合约电量 | contract_info | volume_ratio | 合约电量占比 |
| Y轴-预测出力 | prediction_data | predicted_value | 功率预测值 |
| Y轴-余量 | - | - | 预测出力 - 合约电量 |

**计算逻辑**:
```
时段合约电量 = SUM(contract_info.volume_ratio) × 装机容量 × 时段出力系数
时段预测出力 = prediction_data.predicted_value (功率预测)
时段余量 = 时段预测出力 - 时段合约电量
```

**数据筛选**: decision_date=决策日期

**API接口**: `GET /api/decision/midterm/volume-analysis`

#### 4.2.5 中长期申报策略表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时段 | - | - | 固定值（00:00-24:00，按小时划分） | 交易时段 |
| 电量上限(MWh) | contract_info + prediction_data | - | 时段余量 × 策略系数 | 可编辑 |
| 电价取值区间(元/MWh) | prediction_data | lower_bound ~ upper_bound | 电价预测置信区间 | 可编辑 |
| 概率(%) | prediction_data | - | 策略触发概率 | 系统计算 |
| 交易类型 | - | - | 选中的交易方式 | 显示当前交易类型 |

**编辑交互**:
- 双击"电量上限"和"电价取值区间"单元格可编辑
- 编辑后点击顶部"保存策略"按钮提交

**API接口**: `GET /api/decision/midterm/strategy-table`

#### 4.2.6 价格策略分析图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| X轴 | prediction_data | time_slot | 交易时段（00:00-24:00） |
| Y轴-预测电价 | prediction_data | predicted_value | 电价预测值 |
| Y轴-参考线 | contract_info | price | 合约电价参考线 |

**参考线类型**:
- 平均数：AVG(contract_info.price)
- 最大值：MAX(contract_info.price)
- 最小值：MIN(contract_info.price)

**数据筛选**: decision_date=决策日期，prediction_type='电价预测'

**API接口**: `GET /api/decision/midterm/price-strategy`

#### 4.2.7 中长期收益表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时段 | - | - | 固定值（00:00-24:00，按小时划分） | 交易时段 |
| 日滚动(元/MWh) | contract_info | price | 日滚动合约电价 | 加权平均 |
| 年度(元/MWh) | contract_info | price | 年度合约电价 | 加权平均 |
| 多月(元/MWh) | contract_info | price | 多月合约电价 | 加权平均 |
| 月度(元/MWh) | contract_info | price | 月度合约电价 | 加权平均 |
| 月内(元/MWh) | contract_info | price | 月内合约电价 | 加权平均 |
| 汇总(元/MWh) | - | - | 加权平均电价 | 所有类型加权平均 |

**加权平均计算**:
```
汇总电价 = Σ(各类型合约电量 × 各类型合约电价) / Σ(各类型合约电量)
```

**API接口**: `GET /api/decision/midterm/revenue-table`

---

## 五、现货交易模块

### 5.1 现货市场页面

**页面路径**: `现货交易/现货市场/index.html`
**页面功能**: 现货市场出力及收益分析

#### 5.1.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 日期 | trade_date | DATE | 当前日期 | 交易日期 | 用户输入 |
| 查询按钮 | - | - | - | 触发数据查询 | 用户操作 |

**API接口**: `GET /api/spot/market`

#### 5.1.2 出力及价格曲线图表

**图表类型**: 双Y轴折线图

| 图表元素 | 数据来源表 | 使用字段 | Y轴 | 说明 |
|----------|------------|----------|-----|------|
| X轴 | spot_data | trade_period | - | 15分钟间隔（96点/日） |
| 实际出力 | scada_data | actual_power_mw | 左Y轴 | 实际上网功率，turbine_no='汇总' |
| 日前出清电价 | spot_data | day_ahead_price | 右Y轴 | 日前出清电价 |
| 实时出清电价 | spot_data | real_time_price | 右Y轴 | 实时出清电价 |

**数据筛选**: trade_date=筛选日期

**API接口**: `GET /api/spot/output-price-chart`

#### 5.1.3 收益曲线图表

**图表类型**: 柱状图

| 图表元素 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|----------|------------|----------|----------|------|
| X轴 | spot_data | trade_period | - | 15分钟间隔（96点/日） |
| 日前收益 | spot_data | day_ahead_volume × day_ahead_price | - | 日前出清收益 |
| 实时收益 | spot_data | real_time_volume × real_time_price | - | 实时出清收益 |
| 总收益 | - | - | 日前收益 + 实时收益 | 合计收益 |

**数据筛选**: trade_date=筛选日期

**API接口**: `GET /api/spot/revenue-chart`

---

### 5.2 价格日历页面

**页面路径**: `现货交易/价格日历/index.html`
**页面功能**: 日前及实时电价日历展示

#### 5.2.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 运行月 | trade_month | VARCHAR(7) | 当前月份 | 查询月份，格式YYYY-MM | 用户输入 |

**API接口**: `GET /api/spot/calendar`

#### 5.2.2 日历表格组件

**日历单元格数据**:

| 数据项 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 日期 | spot_data | trade_date | - | 日期 |
| 日前电价 | spot_data | day_ahead_price | AVG(day_ahead_price) | 当日平均日前出清电价 |
| 实时电价 | spot_data | real_time_price | AVG(real_time_price) | 当日平均实时出清电价 |
| 颜色标记 | spot_data | - | 根据电价高低着色 | 热力图颜色 |

**数据筛选**: trade_date在查询月份范围内

**热力图颜色规则**:
- 低电价（< 350元/MWh）：绿色系
- 中等电价（350-450元/MWh）：黄色系
- 高电价（> 450元/MWh）：红色系

**API接口**: `GET /api/spot/calendar-data`

---

### 5.3 现货辅助决策页面

**页面路径**: `现货交易/现货辅助决策/index.html`
**页面功能**: 现货交易策略分析与优化

#### 5.3.1 筛选栏组件

| 组件名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 数据来源 |
|----------|----------|----------|--------|------|----------|
| 载入历史方案 | strategy_id | VARCHAR(50) | - | 历史策略方案ID | strategy_info表 |
| 报价日期 | quote_date | DATE | 当前日期 | 报价日期 | 用户输入 |
| 测算方案 | calc_method | VARCHAR(50) | day-ahead | 日前预测价格/申报出力 | 用户选择 |
| 场站名称 | station_id | VARCHAR(32) | 默认场站 | 场站名称 | station_info表 |
| 报价策略 | quote_strategy | VARCHAR(50) | neutral | 保守策略(P30)/中性策略(P50)/激进策略(P70) | 用户选择 |
| 保存策略按钮 | - | - | - | 保存当前策略方案 | 用户操作 |
| 场站参数配置按钮 | - | - | - | 打开场站参数配置弹窗 | 用户操作 |

**API接口**: `GET /api/spot/decision`

#### 5.3.2 场站参数配置弹窗

| 参数名称 | 数据来源表 | 使用字段 | 说明 |
|----------|------------|----------|------|
| 装机容量（MW） | station_info | installed_capacity_mw | 场站总装机容量 |
| 最小技术出力（MW） | station_info | min_tech_output_mw | 机组最小稳定出力 |
| 爬坡率上限（MW/min） | station_info | ramp_up_rate | 单位时间最大升功率 |
| 爬坡率下限（MW/min） | station_info | ramp_down_rate | 单位时间最大降功率 |
| 启停约束（次/日） | station_info | start_stop_limit | 每日允许启停次数上限 |

**API接口**: `GET /api/station/config`（查询）, `POST /api/station/config`（保存）

#### 5.3.3 出力及价格曲线图表

**图表类型**: 双Y轴折线图

| 图表元素 | 数据来源表 | 使用字段 | Y轴 | 说明 |
|----------|------------|----------|-----|------|
| X轴 | spot_data | trade_period | - | 15分钟间隔（96点/日） |
| 预测出力 | prediction_data | predicted_value | 左Y轴 | 功率预测值 |
| 日前出清电价 | spot_data | day_ahead_price | 右Y轴 | 日前出清电价 |
| 实时出清电价 | spot_data | real_time_price | 右Y轴 | 实时出清电价 |

**数据筛选**: quote_date=报价日期

**API接口**: `GET /api/spot/decision/output-price-chart`

#### 5.3.4 预期收益曲线图表

**图表类型**: 折线图

| 图表元素 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|----------|------------|----------|----------|------|
| X轴 | spot_data | trade_period | - | 15分钟间隔（96点/日） |
| Y轴-预期收益 | - | - | 申报出力 × 申报电价 | 基于策略计算的预期收益 |
| Y轴-基准收益 | - | - | 预测出力 × 日前出清电价 | 基准收益 |

**数据筛选**: quote_date=报价日期

**API接口**: `GET /api/spot/decision/revenue-chart`

#### 5.3.5 批量调整功能组件

**功能说明**: 允许用户对所有时段的申报出力和申报电价进行批量调整

**调整参数**:

| 参数名称 | 字段名称 | 数据类型 | 默认值 | 说明 | 影响范围 |
|----------|----------|----------|--------|------|----------|
| 批量申报出力-倍率 | output_multiplier | DECIMAL(5,2) | 1.0 | 出力调整倍率系数 | 所有时段申报出力 |
| 批量申报出力-偏移 | output_offset | DECIMAL(10,2) | 0 | 出力调整固定偏移量 | 所有时段申报出力 |
| 批量申报电价-倍率 | price_multiplier | DECIMAL(5,2) | 1.0 | 电价调整倍率系数 | 所有时段申报电价 |
| 批量申报电价-偏移 | price_offset | DECIMAL(10,2) | 0 | 电价调整固定偏移量 | 所有时段申报电价 |

**数据交互逻辑**:

| 操作 | 触发条件 | 数据更新 |
|------|----------|----------|
| 批量调整出力 | 用户输入出力倍率和偏移量，点击"批量调整"按钮 | 重新计算所有时段申报出力 |
| 批量调整电价 | 用户输入电价倍率和偏移量，点击"批量调整"按钮 | 重新计算所有时段申报电价 |
| 保存策略 | 用户点击"保存策略"按钮 | 将当前申报方案保存到策略表 |
| 载入历史方案 | 用户选择历史策略方案 | 从策略表读取并恢复申报方案 |

**计算逻辑**:
```
调整后的申报出力 = 原始申报出力 × 出力倍率 + 出力偏移
调整后的申报电价 = 原始申报电价 × 电价倍率 + 电价偏移
```

**数据流向**:
- 批量调整 → 更新申报方案编辑表格 → 更新预期收益曲线图表 → 更新申报汇总组件

**API接口**: `POST /api/spot/decision/batch-adjust`

#### 5.3.6 申报方案编辑表格

| 表格列 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 时段（96点） | spot_data | trade_period | - | 15分钟间隔 |
| 申报出力 | prediction_data | predicted_value | 预测出力 × 策略系数 | 可编辑 |
| 申报电价 | prediction_data | predicted_value | 电价预测值 × 策略系数 | 可编辑 |
| 预测电价 | prediction_data | predicted_value | - | 电价预测值 |

**数据筛选**: quote_date=报价日期

**API接口**: `GET /api/spot/decision/declaration-table`

#### 5.3.7 申报汇总组件

| 汇总项 | 数据来源表 | 使用字段 | 计算逻辑 | 说明 |
|--------|------------|----------|----------|------|
| 总申报出力 | prediction_data | predicted_value | SUM(申报出力) | 单位：MW |
| 平均申报电价 | prediction_data | predicted_value | AVG(申报电价) | 单位：元/MWh |
| 预期总收益 | - | - | Σ(申报出力 × 申报电价 × 0.25) | 单位：元 |
| 均价差 | - | - | AVG(申报电价 - 预测电价) | 单位：元/MWh |

**API接口**: `GET /api/spot/decision/summary`

---

## 六、后台数据维护模块

### 6.1 场站信息管理页面

**页面路径**: `后台数据维护/场站信息管理/index.html`
**页面功能**: 场站基础参数信息的维护管理

#### 6.1.1 页面功能清单

| 功能 | 说明 | 是否支持 |
|------|------|----------|
| 新增 | 手动新增场站信息 | 是 |
| 编辑 | 修改已有场站信息 | 是 |
| 删除 | 软删除场站信息 | 是 |
| 筛选 | 按场站名称、所属